server:
  port: 8080

spring:
  application:
    name: msvc-gateway
  cloud:
    config:
      enabled: false

logging:
   level:
     org.springframework.cloud.gateway: TRACE
     org.springframework.web: DEBUG